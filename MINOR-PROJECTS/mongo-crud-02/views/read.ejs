<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/public/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen pt-5 pl-20 bg-zinc-900  text-white">
        <a class="text-blue-500 text-xl" href="/">Back to Home</a>
        <h1 class="text-3xl tracking-tighter mb-3 mt-3">All User</h1>
        <div class="users flex gap-5 flex-wrap">
            <% users.reverse().forEach(function(user){ %>
                <div class="user w-64 p-4 bg-zinc-800 rounded-lg">
                    <div class="w-full h-40 rounded-lg bg-zinc-700 overflow-hidden">
                        <img class="w-full h-full object-cover object-center" src="<%= user.image %>" alt="">
                    </div>
                    <h3 class="text-xl mt-3 tracking-tighter"><%= user.name %></h3>
                    <h5 class="text-zinc-500"><%= user.email %></h5>
                    <div class="flex gap-5 mt-5">
                        <a class="text-blue-500" href="/edit/<%= user._id %>">Edit User</a>
                        <a class="text-red-500" href="/delete/<%= user._id %>">Delete User</a>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
    <script src="/public/javascripts/script.js"></script>
</body>
</html>